package ${package}.mapper;

import ${package}.entity.${className};
import com.platform.common.BaseMapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * Created in ${datetime}
 */
public interface ${className}Mapper extends BaseMapper<${className}> {
	
}
